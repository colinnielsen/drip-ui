import { Mono } from '../base/typography';
import { Button } from '../ui/button';

const CheckoutFlow = () => {
  return (
    <Button className="bg-secondary-pop py-6">
      <Mono className="uppercase">Checkout</Mono>
    </Button>
  );
};

export default CheckoutFlow;
